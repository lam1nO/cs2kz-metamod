// [cyb] НАШ файл, апстрима здесь нет.
//
// Синк актуальных ников платформы в `Players.Alias` общей kz-БД. Из Alias берут ник `!maptop`/
// `!top` и реплей-бот, а сам форк пишет его только на заходе игрока — сменивший ник в Steam и
// не заходивший оставался в топе со старым. Платформа держит актуальный ник (игра + рефреш
// Steam) и отдаёт дельту `GET /ingest/v1/players/names?since=<курсор>`; мы переписываем ею Alias.
#pragma once

namespace CybAliasSync
{
	// Таймер-насос (реальное время, переживает смену карты). Зовётся один раз при загрузке.
	void Init();
} // namespace CybAliasSync
